の注文履歴

<table class="table">
<thead class="table-light">
  <th>購入日時</th>
  <th>注文個数</th>
  <th>注文ステータス</th>
</thead>
<tbody>

<%= @orders.each do |orders| %>

<tr>
<td><%= orders.created_at %></td>
<% @some = 0 %>
<td><%= orders.order_details.each do |order_details| %>
<% @some = @some + order_details.quantity %>
<% end %>
<%= @some %>
</td>

<td><%= orders.order_status %></td>

</tr>

<% end %>
</tbody>
</table>

<%= paginate @orders %>