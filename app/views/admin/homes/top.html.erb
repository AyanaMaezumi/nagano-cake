注文履歴一覧

<table class="table">
<thead class="table-light">
  <th>購入日時</th>
  <th>購入者</th>
  <th>注文個数</th>
  <th>注文ステータス</th>
</thead>
<tbody>
<% @orders.each  do |orders| %>

<tr>
<td><%= orders.created_at %></td>
<td><%= orders.customers.last_name %><%= orders.customers.first_name %></td>
<td><%= orders.order_details.quantity %></td>
<td><%= orders.order_status %></td>

<td>
<% if items.is_active==true %>
販売中
<% else %>
販売停止中
<% end %>
</td>
</tr>

<% end %>
</tbody>
</table>

<%= paginate @orders %>