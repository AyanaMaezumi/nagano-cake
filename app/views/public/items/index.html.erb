<h2>商品一覧（全<%= @items.count %>件）</h2>


<% @items.each do |items| %>

<%= link_to item_path(items.id) do %>
<%= image_tag items.get_image(100,100) %>
<% end %><br>
<%= items.name %><br>
<%= items.price %><br>

<% end %>

<%= paginate @items %>